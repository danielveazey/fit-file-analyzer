# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environment
# Replace 'venv' with the actual name of your environment directory if different
venv/
# Other common names you might use:
# env/
# .env/
# .venv/

# Database file
# We don't typically track the database itself, just code/schema if needed
database/rides.db
# You might also want to ignore journal files if they appear
database/rides.db-journal

# Generated data files
# Parquet files generated by the app containing ride data
data/
# It's better to ignore the whole directory.
# Alternatively, if you want to keep the dir but ignore parquet:
# data/*.parquet

# OS generated files
.DS_Store
Thumbs.db

# IDE / Editor specific files (Optional)
.idea/
.vscode/

# Log files (If you generate any)
*.log

# Streamlit secrets (if you add cloud deployment secrets later)
.streamlit/secrets.toml